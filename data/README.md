# In questa directory verranno generate e archiviate le matrici e i relativi Ground-truth.
Per generare il dataset, esegui lo script Python presente nella directory `script/`:
`python3 script/dataset_generator.py` (da eseguire possibilmente nella directory root del progetto)